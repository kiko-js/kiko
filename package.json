{
  "workspaces": [
    "packages/*",
    "examples/*",
    "docs"
  ],
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "prepare": "husky",
    "lint": "oxlint .",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check .",
    "build": "bun run --cwd packages/signal build && bun run --cwd packages/dom build && bun run --cwd packages/router build",
    "pretypecheck": "bun run build",
    "typecheck": "tsc --noEmit -p packages/signal/tsconfig.json && tsc --noEmit -p packages/dom/tsconfig.json && tsc --noEmit -p packages/router/tsconfig.json && tsc --noEmit -p examples/basic/tsconfig.json && tsc --noEmit -p examples/ssr/tsconfig.json && tsc --noEmit -p examples/htm/tsconfig.json && tsc --noEmit -p examples/react-portal/tsconfig.json && tsc --noEmit -p examples/tailwind/tsconfig.json",
    "pretest": "bun run build",
    "test": "bun test",
    "presite:build": "bun run build",
    "site:build": "bun run docs/build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "typescript": "^7.0.2"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs,cjs}": [
      "oxfmt --write",
      "oxlint"
    ],
    "*.{json,md,yml,yaml,css,html}": [
      "oxfmt --write"
    ]
  }
}
