---
"@kikojs/dom": minor
"@kikojs/signal": patch
---

Add streaming SSR (`renderToStream`), signal serialization for SSR→hydration state transfer, `<style nonce>` support, and fragment root scope warning
